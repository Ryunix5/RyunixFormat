// Migration script removed — Creao data migration is intentionally deprecated.
// If you need to import data into Supabase, use a custom script that reads from a
// verified Creao export or contact the maintainer for a one-off migration.
console.log('Migration script removed (Creao -> Supabase migration deprecated).');
process.exit(0);

